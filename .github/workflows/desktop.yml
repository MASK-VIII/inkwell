# Unsigned desktop installers for smoke testing (Windows + macOS).
# For release signing, see docs/DESKTOP.md and wire repository secrets + CSC env vars.
name: Desktop build

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  desktop:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    env:
      CSC_IDENTITY_AUTO_DISCOVERY: 'false'
      VITE_INKWELL_CLOUD_SYNC: '1'
      VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
      VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
      VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
      VITE_PADDLE_CLIENT_TOKEN: ${{ secrets.VITE_PADDLE_CLIENT_TOKEN }}
      VITE_PADDLE_ENVIRONMENT: ${{ secrets.VITE_PADDLE_ENVIRONMENT }}
      VITE_PADDLE_PRICE_ID_BASIC: ${{ secrets.VITE_PADDLE_PRICE_ID_BASIC }}
      VITE_PADDLE_PRICE_ID_PRO: ${{ secrets.VITE_PADDLE_PRICE_ID_PRO }}
      VITE_PADDLE_PRICE_ID_UPGRADE: ${{ secrets.VITE_PADDLE_PRICE_ID_UPGRADE }}
      VITE_PADDLE_EDGE_CHECKOUT: ${{ secrets.VITE_PADDLE_EDGE_CHECKOUT }}
      VITE_PADDLE_CHECKOUT_EBOOK_SUITE: ${{ secrets.VITE_PADDLE_CHECKOUT_EBOOK_SUITE }}
      VITE_PADDLE_CHECKOUT_PRO: ${{ secrets.VITE_PADDLE_CHECKOUT_PRO }}
      VITE_PADDLE_CHECKOUT_UPGRADE: ${{ secrets.VITE_PADDLE_CHECKOUT_UPGRADE }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # When repository secrets are set, installers include cloud login (same VITE_* names as .env.local).
      # Fork PRs usually omit secrets → offline-only build (still green).
      - name: Build desktop bundle
        shell: bash
        run: npm run build:desktop

      - name: Verify latest.yml (Windows NSIS + GitHub publish config)
        if: matrix.os == 'windows-latest'
        shell: bash
        run: |
          if [ ! -f release/latest.yml ]; then
            echo "::error::Missing release/latest.yml — add package.json build.publish (GitHub) for electron-updater."
            ls -la release/ || true
            exit 1
          fi

      - name: Upload release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: inkwell-desktop-${{ matrix.os }}
          path: release/*
